services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: delight
    ports:
      - "5432:5432"
    volumes:
      - delight-postgres:/var/lib/postgresql/data
volumes:
  delight-postgres:
